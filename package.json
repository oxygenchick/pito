{
  "name": "pito",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.mjs",
    "build:web": "node scripts/build-web.mjs",
    "signing:init": "node scripts/init-signing.mjs",
    "build:android": "node scripts/build-web.mjs && cd android && gradlew.bat assembleRelease"
  }
}
